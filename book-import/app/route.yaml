apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: book-import
  labels:
    name: book-import
spec:
  path: "/"
  to:
    kind: Service
    name: book-import
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect